# Root-owned private contract overlay.
# Install as /opt/postersplus/env/postersplus-v2-private.env with mode 0600.

# Keeping the shared request secret in this isolated overlay lets operators
# rotate the v2 contract without changing any public legacy tunnel settings.
POSTERSPLUS_BINGECAT_REQUEST_SECRET=
